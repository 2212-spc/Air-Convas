我将执行 **"深度整合方案"**，将 PPT 控制器无缝集成到 AirCanvas 主程序中，实现单摄像头、单进程的平滑切换。

### 执行计划

1.  **重构 PPT 控制器 (`modules/ppt_gesture_controller.py`)**
    *   **解耦**: 剥离摄像头读取和 MediaPipe 初始化逻辑，使其成为一个纯粹的"逻辑处理器"。
    *   **重命名**: 将 `HandController` 重命名为 `PPTGestureController` 以避免命名冲突。
    *   **接口适配**: 新增 `process_landmarks` 方法，支持接收外部传入的手部关键点数据。

2.  **增强主程序 (`main.py`)**
    *   **模式管理**: 引入全局模式状态 (`APP_MODE`)，支持 `"DRAW"` (绘图) 和 `"PPT"` (演示) 两种模式。
    *   **数据适配**: 创建一个轻量级适配器，将主程序的 `HandDetector` 数据转换为 PPT 控制器所需的格式（补充 z=0）。
    *   **交互集成**:
        *   添加 **Tab 键** 作为模式切换快捷键。
        *   在屏幕左上角显著位置显示当前模式状态。
        *   PPT 模式下禁用绘图、粒子等不相关功能，专注于翻页和激光笔。

3.  **优化与清理**
    *   确保 PPT 模式下的激光笔与绘图模式下的逻辑不冲突。
    *   复用底层的 `HandDetector`，避免重复加载 AI 模型，降低 CPU/GPU 占用。

完成后，您只需运行 `python main.py`，即可通过按 `Tab` 键在"隔空绘画"和"PPT控制"之间自由切换。