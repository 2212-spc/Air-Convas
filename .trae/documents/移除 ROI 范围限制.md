好的，我将移除刚才添加的 ROI 范围限制，恢复全屏映射。

### 修改计划

1.  **移除 ROI 常量定义**: 删除 `ROI_X_MIN`, `ROI_X_MAX` 等配置。
2.  **移除视觉反馈**: 删除视频画面中绘制绿色 "Active Area" 矩形框的代码。
3.  **恢复全屏映射逻辑**: 将坐标转换逻辑还原为直接将摄像头视野 (0~1) 映射到屏幕分辨率。

这样做后，您将可以使用摄像头的全部视野进行控制，不再受中间区域的限制。捏合算法优化和激光笔模式将保留。